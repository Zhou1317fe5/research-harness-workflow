# Remote Run Control

`rrctl` provides remote workload staging, launch, health observation, recovery, and artifact collection.

Distribution: `remote-run-control`. Python package: `remote_run_control`.
Command entrypoint: `rrctl`.
